{
  "name": "box-or-bust",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/app.mjs",
    "check": "node --check server/app.mjs && node --check server/db.mjs && node --check server/migrate.mjs",
    "db:migrate": "node server/migrate.mjs"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "openid-client": "^6.8.1",
    "pg": "^8.16.3"
  }
}
